# Core server
fastapi
uvicorn[standard]
python-multipart

# Pose estimation
rtmlib
onnxruntime-gpu
opencv-python
numpy

# Auth
python-jose[cryptography]
passlib[argon2]
argon2-cffi
python-dotenv

# Database
pymongo[srv]
motor                        

# Streamlit UI
streamlit
streamlit-webrtc
av
streamlit-calendar
streamlit-cookies-controller

# AI Chatbot
langchain
langchain-google-genai
google-generativeai
google-genai

# Charts (Streamlit dashboard)
plotly

--extra-index-url https://download.pytorch.org/whl/cu130
torch
torchaudio

# LLM
langchain-anthropic
langgraph
langgraph-checkpoint-mongodb

# Friday AI Assistant - STT (Silero VAD + CrispASR subprocess - no Transformers)
# torch / torchaudio kept above for Silero VAD only
# transformers, accelerate, datasets removed (Whisper removed)
sounddevice>=0.4.6
scipy>=1.11.0
soundfile>=0.12.1

# TTS (Voxtral via voxtral-mini-realtime-rs subprocess - local sounddevice playback)
# No extra Python packages needed; httpx kept for any remaining HTTP call sites
httpx